document.addEventListener('DOMContentLoaded',()=>{const f=document.getElementById('bot-form');if(!f) return;f.addEventListener('submit',e=>{e.preventDefault();const data=Object.fromEntries(new FormData(f).entries());const out=document.getElementById('bot-output');out.innerHTML=`<h3>Resumen</h3><p><strong>Cliente:</strong> ${data.name} (${data.email})</p><p><strong>Proyecto:</strong> ${data.type}</p><p><strong>Funciones:</strong> ${data.features}</p><p><strong>Plataforma:</strong> ${data.platform}</p><p><strong>Presupuesto:</strong> ${data.budget}</p><p><strong>Tiempo:</strong> ${data.timeline}</p>`;let orders=JSON.parse(localStorage.getItem('dd_orders')||'[]');orders.push({id:Date.now(),spec:data});localStorage.setItem('dd_orders',JSON.stringify(orders));});});